package cars;

public class Registry {
    public static void submit(Registration reg) {
        clean(reg);
        save(reg);
    }

    private static void clean(Registration reg) {
        //do something...
    }

    private static void save(Registration reg) {
        //do something...
    }
}

